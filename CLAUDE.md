# 经销商核企测评 · 项目规则

> 基于全局 CLAUDE.md 规则 + 本项目特化
> 规则版本：v0.1

---

## 关于本项目

银行供应链金融场景下的经销商-核心企业信用评价系统。
接收分行上报名单，经过校验+评级后输出准入结论，留存审计台账。

**核心约束：** AI 不打分、不做最终授信决策。评分由规则引擎计算，LLM 仅做文字组织。

---

## 目录结构

```
经销商核企测评/
├── CLAUDE.md              ← 本文件，项目专属规则
├── ARCHITECTURE.md        ← 架构设计文档
├── input/                 ← 原始输入（分行名单CSV等）
├── output/                ← 产出文件（评价报告、分类清单）
├── logs/                  ← 运行日志（按 YYYY-MM 分目录）
├── scripts/               ← 工具脚本（评分引擎、解析器）
├── knowledge/             ← 知识库（规则文件、白名单、品牌库）
│   ├── industry_whitelist.md
│   ├── forbidden_business_types.md
│   ├── brand_authority_library.md
│   └── redline_rules.md
├── config/                ← 配置文件（评分权重、阈值）
├── templates/             ← 报告模板
├── prompts/               ← 提示词模板
├── tests/                 ← 测试数据集（demo案例库）
├── memory/                ← 记忆存储（企业判定记录、复核历史）
└── references/            ← 参考材料（外部文档备份）
```

---

## 命名规则

- 输入文件：`{批次描述}_{日期}.{csv|xlsx}`
- 输出报告：`{企业A}_{企业B}_评价报告_{日期}.md`
- 日志文件：`核企判定_{batch_id}_{YYYYMMDD_HHMMSS}.jsonl`
- 记忆文件：`{企业名}_判定记录.md`（正斜杠用_替代）

---

## 工作流

```
导入 → 校验 → 评级 → 归档 → 追溯
         │
         ├─ 模块2.1：主体资质校验
         ├─ 模块2.2：商品品牌校验
         ├─ 模块2.3：行业校验
         └─ 模块2.4：经销体系校验
```

步骤说明见 `ARCHITECTURE.md` 中 L1/L2/L3 三层颗粒度定义。

---

## 红线规则

| 红线 | 后果 |
|------|------|
| 评分依赖 LLM 拍数（不用规则引擎） | ❌ 禁止，违反监管要求 |
| 红线企业判定为通过 | ❌ 0容忍 |
| 静默覆盖已有记忆/文件 | ❌ 0容忍 |
| 敏感信息被持久化 | ❌ 0容忍 |
| 自动重试超过 1 次 | ❌ 全局规则 |
| 静默等待超过 10 秒 | ❌ 全局规则 |
| 自己做影响文件结构的决定 | ❌ 必须先问 |

---

## 项目状态

> 当前阶段：初始搭建（v0.1 原型期）
> 下一阶段：Claude Code 跑通完整流程 → Agent 平台迁移
