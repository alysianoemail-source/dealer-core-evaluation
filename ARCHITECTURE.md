# 经销商核企测评 · 系统架构

> 版本：v0.1 | 最后更新：2026-07-23
> 环境：Claude Code → 未来迁移至 Agent 平台
> 术语说明：本文档中「Agent」指 Claude Code 中运行的自动化流程

---

## 目录

1. [L1/L2/L3 三层颗粒度](#l1l2l3-三层颗粒度)
2. [输入源（01）](#01-输入源)
3. [工具链（02）](#02-工具链)
4. [失败处理（03）](#03-失败处理)
5. [日志规范（04）](#04-日志规范)
6. [版本管理（05）](#05-版本管理)
7. [记忆清单](#记忆清单)
8. [风险检查点](#风险检查点)
9. [验收标准](#验收标准)

---

---

## L1/L2/L3 三层颗粒度

> **重要澄清：L1 / L2 / L3 不是任务的递进阶段，是描述同一件事的不同颗粒度。**
> 从万米高空俯瞰到贴地细看，三层逐级放大。

### L1 — 粗粒度（流程主干）

用 5 个动词概括整件事：

```
导入  →  校验  →  评级  →  归档  →  追溯
```

| 动词 | 一句话 |
|------|--------|
| **导入** | 批量接收分行上报名单，解析为结构化数据 |
| **校验** | L1基础初筛 + L2四维深度打分，双轮判定 |
| **评级** | 综合分数映射 A/B/C/D，输出准入结论 |
| **归档** | 分类结果推送对应分行，台账留存 |
| **追溯** | 全流程判定记录 + 风险证据可审计可回溯 |

---

### L2 — 中粒度（每个步骤拆为输入→输出模块）

#### 步骤①：导入

```
输入                            →  输出
┌─────────────────────────┐    ┌─────────────────────────┐
│ 分行上传的 Excel/CSV     │    │ 结构化企业数据列表       │
│ 核企名单                │ →  │ 逐条解析、字段标准化     │
│ 包含：企业名称/信用代码  │    │ 必填字段校验完成         │
│ 主营产品/行业/渠道简介   │    │ 重复/缺失行已标注        │
└─────────────────────────┘    └─────────────────────────┘
```

#### 步骤②：校验（核心）

拆为 4 个子模块：

```
┌─ 模块 2.1：主体资质校验 ─────────────────────────────────┐
│ 输入                         →  输出                      │
│ · 工商信息（启信宝/企查查）   │ · 主体资质结论              │
│ · 司法信息（失信/被执行/处罚）│   （通过/不通过/待复核）     │
│ · 负面清单（行内黑名单）      │ · 红线触发标记              │
│ · 舆情信息（重大负面）        │                            │
└──────────────────────────────┴────────────────────────────┘

┌─ 模块 2.2：商品品牌校验 ─────────────────────────────────┐
│ 输入                         →  输出                      │
│ · 主营商品品类               │ · 商品品牌结论              │
│ · 品牌授权证明               │   （合作品类/非合作/待确认） │
│ · 品牌价值分级（头部/中端/杂牌）│ · 商品合规性标记            │
│ · 3C认证/质检报告             │                            │
└──────────────────────────────┴────────────────────────────┘

┌─ 模块 2.3：行业校验 ────────────────────────────────────┐
│ 输入                         →  输出                      │
│ · 企业所属行业               │ · 行业校验结论              │
│ · 行业白名单（鼓励/限制/禁止）│   （通过/待复核/不通过）     │
│ · 行业生命周期/政策评估       │ · 行业风险标记              │
└──────────────────────────────┴────────────────────────────┘

┌─ 模块 2.4：经销体系校验 ─────────────────────────────────┐
│ 输入                         →  输出                      │
│ · 经销商名录                │ · 体系校验结论              │
│ · 渠道覆盖/门店数量          │   （成熟/待确认/不成熟）     │
│ · 上下游合作方               │ · 集中度/关联自融风险标记    │
│ · 历史经营流水              │                            │
└──────────────────────────────┴────────────────────────────┘
```

#### 步骤③：评级

```
输入                            →  输出
┌─────────────────────────┐    ┌─────────────────────────┐
│ L1初筛结论              │    │ 合格  → 批量上报+评分    │
│ L2四维评分数据          │ →  │ 不合格 → 逐条拦截原因    │
│ 评分权重配置             │    │ 待复核 → 列出存疑+推送   │
└─────────────────────────┘    └─────────────────────────┘
```

评级映射：A ≥ 80 / B 70-79 / C 60-69 / D < 60

#### 步骤④：归档

```
输入                            →  输出
┌─────────────────────────┐    ┌─────────────────────────┐
│ 最终分类定级结果         │    │ 分行分类清单（推送）     │
│ 全流程判定记录           │ →  │ 审计台账（归档）         │
│ 风险证据材料             │    │ 记忆写入                 │
└─────────────────────────┘    └─────────────────────────┘
```

#### 步骤⑤：追溯

```
输入                            →  输出
┌─────────────────────────┐    ┌─────────────────────────┐
│ 企业名称/批次ID         │    │ 全版本判定历史           │
│ 查询条件                │ →  │ 原始评分+复核记录        │
│                         │    │ 责任人+时间戳            │
└─────────────────────────┘    └─────────────────────────┘
```

---

### L3 — 细粒度（一条具体规则）

从**步骤②校验 > 模块 2.1 主体资质校验**中挑一条红线判定规则：

```
规则编号：R002
所属模块：L2 模块 2.1 主体资质校验
触发层级：一票否决级

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
规则：企业若存在 "重大税收违法失信主体" 记录，
      无论 L2 其他维度评分多高，维度三（核企自身信用）
      直接归零，综合评级锁定为 D（不通过）。
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

判断逻辑：

  1. 从司法信息中检索该企业是否出现在"重大税收违法"名单
     （数据源：启信宝/企查查/税务披露 → P1 级别数据）
  2. 若命中 → 维度三评分 = 0，综合评级 = D
  3. 评级结论标注"一票否决：重大税收违法失信"
  4. 该条判定在结果清单中红色高亮，不允许批量上报
  5. 在审计台账中标记"红线案件，永久保留"

示例（来自演示案例库）：
  远辰精密化工有限公司 × 鑫达供应链有限公司
  L2维度一基本面：87 分（行业基本面较强）
  → 但因触发重大税收违法红线
  → 维度三归零（d3: 0）
  → 结论：不通过（E5）

权重公式涉及的版本参数：
  weights.v0.1:
    - 维度一（行业景气）: 15%
    - 维度二（品牌竞争力）: 15%
    - 维度三（核企自身信用）: 40%  ← 本条规则命中的维度
    - 维度四（经销体系健康度）: 30%
```

---

## 01 输入源

### 1.1 来源总览

| # | 来源 | 内容 | 格式 | 频率 | 产权/信任等级 |
|---|------|------|------|------|--------------|
| A | **分行上报名单** | 批量经销商-核企对 | CSV / TXT / Excel | 批次驱动 | 内部数据，高信 |
| B | **业务系统对接** | 工商、司法、税务接口 | API JSON | 实时/定时 | 三方数据，需验证 |
| C | **知识库 / 参考文件** | 行业白名单、禁止类行业目录、品牌授权库 | MD / JSON | 人工维护 | 内部治理，中高信 |
| D | **人工录入 / 补充材料** | 复核意见、补充财报、授权书照片 | Text / Image / PDF | 复核时触发 | 人工确认，中高信 |
| E | **搜索引擎** | 舆情、品牌口碑、行业价格指数 | Web Page | 实时 | 公开信息，需交叉验证 |

### 1.2 各来源详细规格

#### A — 分行上报名单

```
触发方式：用户上传文件（CSV/TXT/Excel）
文件约定：
  CSV 编码：UTF-8 with BOM（Excel 兼容）
  分隔符：逗号 或 Tab（自动检测）
  必填列：核心企业名称, 经销商名称
  可选列：统一社会信用代码(核企), 统一社会信用代码(经销商), 主营产品, 所属行业

解析流程：
  1. 读取原始文件 → 逐行解析
  2. 自动检测表头行（含"核心企业/核企"关键词）
  3. 去掉空行、BOM头、不可见字符
  4. 去重（按 核企名称+经销商名称 组合去重，保留第一条）
  5. 输出：结构化列表 [{core_name, dealer_name, credit_code_core?, credit_code_dealer?, product?, industry?}]

失败场景：
  · 空文件 → 拒绝并提示
  · 所有行都未命中 → 提示"未匹配到任何已知企业"
  · 部分行缺少必填字段 → 标记"信息不完整"，继续处理其他行
```

#### B — 业务系统接口（MCP / API）

```
当前可用工具（来自 .mcp.json 和现有 skill）：
  ┌─────────────────────────────┬────────────────────────────────────┐
  │ 接口                        │ 用法                               │
  ├─────────────────────────────┼────────────────────────────────────┤
  │ 启信宝 / 企查查（人工导出）   │ 工商信息、股权穿透、司法记录        │
  │ WebSearch + WebFetch        │ 舆情搜索、行业价格指数、品牌口碑    │
  │ chrome-devtools MCP         │ 需要时浏览器自动化采集公开数据      │
  │ feishu MCP                  │ 飞书通知/结果推送                  │
  │ 本地文件系统 (Read/Glob)    │ 读已有报告、知识库、模板           │
  └─────────────────────────────┴────────────────────────────────────┘

待接入规划（Agent 平台阶段）：
  · 天眼查/启信宝 API 直连（企业工商 + 司法涉诉）
  · Wind / Choice 数据接口（行业财务 + 大宗价格）
  · 信通院 / 统计局公开数据定时抓取

调用优先级规则（参考 credit-analysis.md）：
  P0 = 官方/一手/受控（内部系统、监管披露）→ 直接采用
  P1 = 三方合规数据（启信宝/企查查）→ 采用，标注来源
  P2 = 公开数据（统计局/信通院/行业协会）→ 采用，交叉验证
  P3 = 搜索引擎结果（舆情/新闻）→ 采用，要求至少两个独立来源交叉验证
  禁止：自媒体、营销软文、二次转载无法溯源的数据
```

#### C — 知识库文件

```
存放位置：经销商核企测评/knowledge/
  ├── industry_whitelist.md        ← 银行白名单行业目录（鼓励/限制/禁止）
  ├── forbidden_business_types.md  ← 禁止类经营主体（个体户/空壳/挂靠等）
  ├── brand_authority_library.md   ← 品牌分级库（头部/区域/杂牌）
  └── redline_rules.md             ← 一票否决红线清单（更新日志在文件头）

更新方式：人工维护，每次更新在文件头增加 changelog 条目
引用方式：Agent 读入后缓存为 session 变量，作为判定依据
```

#### D — 人工录入补充材料

```
触发时机：L2 待复核阶段，或 Agent 判定"资料不足"时
输入方式：用户粘贴文本 或 上传 PDF/图片
处理工具：
  · PDF → Markitdown / python-docx / pdfplumber 提取文本
  · 图片（营业执照/授权书）→ OCR（python-tesseract 或 LLM视觉能力）
  · 审计财报 → 结构化关键指标提取

输出：补充字段注入当前判定记录
  字段：{supplement_type, content, source_file, verified_by, verified_at}
```

---

## 02 工具链

### 2.1 工具清单

| # | 工具 | 具体实现 | 在哪个步骤用 | 失败降级 |
|---|------|---------|-------------|---------|
| # | 工具 | 具体实现 | 用在哪个 L1 步骤 | 失败降级 |
|---|------|---------|-----------------|---------|
| T1 | **文件解析** | CSV：Python csv / 标准库；Excel：openpyxl；Docx：python-docx | 导入 | 格式不识别→提示用户转CSV |
| T2 | **OCR / 图片提取** | python-tesseract 或 LLM 视觉能力（图片→文字） | 校验（补充材料时） | OCR失败→提示用户手动输入 |
| T3 | **PDF 解析** | Markitdown（推荐）或 pdfplumber / PyMuPDF | 校验（补充材料时） | 扫描件→走OCR；加密→要求解密 |
| T4 | **知识检索** | 本地文件 Read（knowledge/目录）+ 关键词匹配 | 校验（引用规则/白名单） | 未找到→明确告知用户"该规则未配置" |
| T5 | **工商司法查询** | 人工导出文件（现阶段）；未来→企业API | 校验（模块 2.1 主体资质） | 无数据→标记"待人工核实" |
| T6 | **舆情搜索** | WebSearch（搜索引擎） | 校验（模块 2.1 主体资质） | 无结果→标注"未检索到相关舆情" |
| T7 | **规则引擎** | Python脚本（本地执行评分计算、红线判定、区间校验） | 校验 + 评级 | 规则冲突→报错终止并提示 |
| T8 | **LLM 生成** | Claude Code 模型能力 | 校验（摘要）+ 评级（描述）+ 追溯（查询） | Token耗尽→分段重试 |
| T9 | **通知推送** | feishu MCP（飞到飞书群）；本地→文件写入+终端提示 | 归档 | 飞书不可用→降级为本地文件归档 |
| T10 | **记忆读写** | 本地 Markdown 文件（`memory/` 目录） | 归档 + 追溯 | 文件损坏→重建并提示 |

### 2.2 数据流向

```
                    ┌──────────────────────────────┐
                    │         T4 知识检索            │
                    │  (行业白名单/品牌库/红线规则)   │
                    └─────────────┬────────────────┘
                                  │
┌───────────┐    ┌────────────────▼──────────────────┐    ┌───────────┐
│ T1 文件解析 │ → │         T7 规则引擎               │ → │ T8 LLM    │
│ (CSV/Excel)│   │  (校验 + 评级，评分全靠规则引擎)    │   │ (生成报告) │
└───────────┘    └──────────▲──────────────▲──────────┘   └─────┬─────┘
                            │              │                    │
                   ┌────────┴───┐    ┌─────┴────────┐          │
                   │ T5 工商司法 │    │ T6 舆情搜索  │          │
                   │ (企查查等)  │    │ (WebSearch)  │          │
                   └────────────┘    └──────────────┘          │
                                                                │
┌───────────┐    ┌──────────────────────────────────────┐       │
│ T2 OCR    │ → │       T3 PDF 解析                    │       │
│ (图片→文) │   │  (Markitdown/pdfplumber)             │       │
└───────────┘    └──────────────┬───────────────────────┘       │
                                │ (仅待复核/资料不足时触发)      │
                                └───────────────────────────────┘
                                                         ↓
                    ┌─────────────────────────────────────────┐
                    │  T10 记忆读写 (持久化) + T9 通知 (飞书)  │
                    │        归档 + 追溯                       │
                    └─────────────────────────────────────────┘
```

### 2.3 各 L1 步骤 → 工具映射

| L1 步骤（动词） | 使用的工具 | 备注 |
|----------------|-----------|------|
| **导入** | T1 文件解析 | 分行名单 → 结构化列表 |
| **校验** | T4+T5+T6+T7 + 必要时 T2+T3 | 四个 L2 模块并行或串行校验，评分靠规则引擎 |
| **评级** | T7 规则引擎 + T8 LLM生成 | 评分用规则引擎，LLM只做结论的文字组织 |
| **归档** | T9 通知推送 + T10 记忆读写 | 结果分类推送 + 持久化 |
| **追溯** | T10 记忆读写 + T8 LLM生成 | 查询历史记录，生成对比摘要 |

> **红线一：评分不依赖 LLM 拍数。** LLM 只做自然语言组织（摘要、结论描述），评分数据必须由规则引擎 T7 计算产生。参考现有评价报告最后一页声明："AI 不打分、不做最终授信决策"。

---

## 03 失败处理

### 3.1 失败矩阵

| 失败场景 | 发生阶段 | 症状 | 处理方式 | 对用户的提示 |
|---------|---------|------|---------|------------|
| **F1 文件格式不识别** | 导入 | 解析返回空/乱码 | 拒绝该文件，提示支持的格式 | ❌ "文件格式无法识别，请确认是 CSV（UTF-8编码）或 Excel (.xlsx)" |
| **F2 全部行未命中** | 导入 | 0条匹配白名单/知识库 | 终止本轮，建议人工排查 | ❌ "导入 0 条有效记录。请检查名单格式：每行需包含 核心企业名称 + 经销商名称" |
| **F3 部分数据缺失** | 导入 | 缺少统一社会信用代码/行业 | 标记"信息不完整"，继续处理其他行 | ⚠️ "第N行缺少[字段名]，该条标记为'信息不完整'，需要人工补充后重新导入" |
| **F4 知识库未配置** | L1/L2 | knowledge/ 目录缺少关键文件 | 跳过该规则，标注"规则未配置" | ⚠️ "[规则名] 未配置，该维度判定跳过。请补充 knowledge/ 下对应文件" |
| **F5 工商司法查询无结果** | L2 | 外部数据源查不到该企业 | 标记"无数据"，进入待复核 | ⚠️ "未查到[企业名]的工商/司法信息，可能原因：企业名称不准确、非独立法人、已注销" |
| **F6 舆情搜索失败** | L2 | WebSearch 网络错误/无结果 | 标注"未检索到相关舆情"，不阻断流程 | ⚠️ "舆情数据暂无法获取（网络原因或该企业公开信息较少），建议人工补充" |
| **F7 规则引擎异常** | L1/L2评分 | 规则冲突/除零/越界 | 中断该条评分，输出已算部分 | ❌ "[企业名]评分异常：规则[规则编号]冲突，已输出部分可用分数，其余标记待复核" |
| **F8 LLM Token 耗尽** | 报告生成 | 输出截断 | 分段重试，若仍失败则输出已有内容 | ⚠️ "报告生成过程中 Token 耗尽，已输出已完成部分，剩余内容请重新触发生成" |
| **F9 记忆写入失败** | 归档 | 文件写锁/磁盘满 | 内存中保留，下次启动时重试 | ❌ "记忆写入失败（[原因]），当前数据保留在会话中，下次启动自动重试写入" |
| **F10 飞书推送失败** | 通知 | API 不可用/权限失效 | 降级为本地文件归档 + 终端提示 | ⚠️ "飞书推送失败，结果已保存在本地。[文件路径]" |

### 3.2 失败处理原则

```
分级处理：
  ❌ = 阻断级 → 必须人工介入后才能继续
  ⚠️ = 告警级 → 继续跑，但标记问题让用户知道

不允许的行为：
  · 静默跳过（用户不知道出过问题）
  · 默认给分（数据缺失时直接猜分数）
  · 自动重试超过 1 次（见 CLAUDE.md 全局规则）
```

### 3.3 模型不确定性处理（LLM 特有）

当以下情况出现时，LLM 不应自己猜测：

| 场景 | 处理方式 |
|------|---------|
| 企业行业归属模糊（跨行业经营） | 输出"跨行业经营"标签，列出涉及行业，不自行选择其一 |
| 品牌知名度无法判断 | 在报告中标注"品牌影响力未知，建议人工核实" |
| 舆情摘要包含矛盾信息 | 并列展示正反两面，不自行"偏向"某一方 |
| 规则未覆盖的情形 | 输出"当前规则未覆盖此情形"，转人工复核 |

---

## 04 日志规范

### 4.1 单次任务日志结构

```
logs/YYYY-MM/
  └── 核企判定_{batch_id}_{YYYYMMDD_HHMMSS}.jsonl
```

每条日志为 JSONL 的一行，包含：

```json
{
  "batch_id": "B20260723_001",
  "event_id": "EVT-001",
  "timestamp": "2026-07-23T14:30:00+08:00",
  "event_type": "import|validate|scoring|supplement|report|review|archive",
  "trigger": {
    "user": "user_name",
    "action": "upload_file | run_command | manual_review",
    "session_id": "session_xxx"
  },
  "input": {
    "file_name": "xxx.csv",
    "total_rows": 100,
    "valid_rows": 95,
    "invalid_rows": 5
  },
  "output": {
    "summary": "通过85条 / 待复核8条 / 不通过2条 / 信息不完整5条",
    "report_path": "output/xxx.md",
    "memory_path": "memory/xxx.md"
  },
  "confirmation": {
    "reviewed_by": null,
    "reviewed_at": null,
    "review_status": "pending"
  },
  "errors": [
    {"type": "data_missing", "row": 12, "detail": "缺少统一社会信用代码", "resolution": "skipped"}
  ],
  "version": {
    "rules": "v0.1",
    "templates": "v0.1",
    "prompts": "v0.1"
  }
}
```

### 4.2 谁必须记录

| 操作 | 记录内容 |
|------|---------|
| 用户上传文件 | 文件名、行数、有效/无效行数 |
| Agent 执行判定 | 每条判定的输入摘要、评分结果、引用规则版本 |
| 用户发起复核 | 复核人、复核意见、修改内容、修改时间 |
| 系统异常 | 异常类型、堆栈摘要、影响范围 |
| 规则/模板更新 | 更新内容、更新人、更新时间、版本号变更 |

### 4.3 日志保留策略

- 在线保留：最近 90 天（按日期分目录）
- 归档：每季度打包一次存入 `logs/archive/`
- 清理：超过 2 年的日志自动删除
- 红线日志（一票否决案件）：永久保留，单独标记

---

## 05 版本管理

### 5.1 需要版本化的资产

| 资产 | 文件位置 | 版本号位置 | 更新方式 |
|------|---------|-----------|---------|
| **判定规则** | `knowledge/` 下各规则文件 | 文件头 `version: v0.1` | 人工修改 + changelog |
| **评分权重配置** | `config/scoring_weights.json` | 文件内 `version` 字段 | 人工修改 + changelog |
| **报告模板** | `templates/report_template.md` | 文件名后缀 `_v01` | 人工修改 |
| **提示词** | `prompts/` 目录 | 文件头 `version: v0.1` | 同规则管理 |
| **评测集** | `tests/test_cases.json` | 文件内 `version` 字段 | 随案例积累更新 |

### 5.2 版本号规则

```
格式：v{主版}.{次版}
主版：规则结构/权重体系变更时 +1
次版：规则细节/阈值调整、模版优化时 +1

示例：
  v0.1 → 初始版本
  v0.2 → 修改了四维评分中维度三的权重
  v1.0 → 首次正式发布

每个版本的 changelog 必须包含：
  · 变更内容
  · 变更原因
  · 变更人
  · 变更日期
  · 影响范围（哪些规则/流程受影响）
```

### 5.3 版本记录格式示例

```markdown
# 行业白名单目录

> version: v0.2
> last_updated: 2026-07-23
> maintainer: [角色/人名]

## Changelog

### v0.2 (2026-07-23)
- 变更：将"储能设备"从"鼓励类"移至"限制类"
- 原因：行业产能过剩风险上升，监管收紧
- 变更人：[人名]

### v0.1 (2026-07-01)
- 初始版本，基于银行现行白名单目录建立
```

---

## 记忆清单（修订版）

> 基于全局 CLAUDE.md 记忆规则 + 本项目特点定制

### 必须记（持久化，Append-only）

| 类别 | 具体记什么 | 数据结构 | 写入时机 | 写入者 |
|------|-----------|---------|---------|-------|
| **企业判定记录** | 企业对名称、校验结论、四维评分、总分、评级、红线触发情况、判定时间、规则版本 | 每条一对→一个记忆文件 | 判定完成时 | Agent自动 |
| **人工复核记录** | 复核人、复核时间、原始评分、修正后评分、修正原因、补充材料路径 | 追加到原判定记录的复核字段 | 用户确认提交时 | Agent在用户指令后写入 |
| **已完成报告索引** | 报告标题、文件路径、生成日期、包含哪些企业、评分版本 | 一个索引文件 + 逐条引用 | 报告生成后 | Agent自动 |
| **数据使用记录** | 该批次用了哪些数据源、哪些字段缺失、哪些行失败 | 追加到批次日志 | 任务结束时 | Agent自动 |
| **用户偏好（项目级）** | 报告输出格式偏好、常关注的行业/企业、偏好的通知方式 | 一个偏好文件 | 用户表达明确偏好时 | 用户同意后写入 |

### 临时记（会话级，不持久化）

| 类别 | 记什么 | 为什么 |
|------|--------|-------|
| **当前批次进度** | 正在处理第几条/共几条，当前步骤 | 断点续接（同一会话内） |
| **中间评分明细** | 四维各项原始评分 | 最终分已算出后即可丢弃 |
| **WebSearch 会话缓存** | 本批次搜过的企业舆情结果 | 避免重复搜索（同一会话内） |
| **用户本次指令流** | 用户说了什么、做了什么选择 | 上下文连续性 |

### 坚决不记

| ❌ 类别 | 原因 |
|---------|------|
| 个人敏感信息（身份证/手机号/银行账号） | 合规红线 |
| LLM 原始对话日志 | 超出项目范围，且无用 |
| API密钥/Token/密码 | 安全风险，走 `.mcp.json` 或环境变量 |
| 可以重新计算出来的中间值 | 冗余存储 |
| 其他项目/无关对话信息 | 记忆污染 |

### 写入与删除规则

```
写入：
  · Append-only：不覆盖已有记录，新版本追加
  · 每条记录包含 {created_at, created_by, version, content}
  · 写入前校验必填字段，缺字段则拒绝写入并提示

更新（修正）：
  · 标记原记录为 superseded_by: {new_version_id}
  · 写入新版本关联到原记录
  · 禁止直接修改已有记录的内容

删除：
  · 只支持软删除：添加 deleted_at + deleted_reason 字段
  · 物理删除需要用户二次确认 + 输入原因
  · 提供 /记忆恢复 命令

查询：
  · 默认只返回最新版本 + 未删除的记录
  · 提供 --history 标志查看全版本
  · 提供 --include-deleted 标志查看已删除记录
```

---

## 风险检查点（修订版）

### 检查点①：导入质量门

```
位置：T1 文件解析完成后
触发条件：每次导入

检查项：
  □ 文件格式 → 是支持的格式（CSV/Excel/TXT）？→ 否→❌阻断
  □ 是否有数据 → 解析后行数 > 0？→ 否→❌阻断
  □ 必填字段 → 每行都有核心企业+经销商名称？→ 否→⚠️标记缺失行
  □ 重复检查 → 是否有完全重复的行？→ 去重+日志记录
  □ 编码检查 → UTF-8无乱码？→ 否→尝试自动转码，失败→❌阻断

通过标准：有效数据行 > 0
拦截后果：部分数据缺失的行标记"信息不完整"，不进入判定管线
```

### 检查点②：判定一致性门

```
位置：T7 规则引擎产出评分后，T8 LLM生成报告前
触发条件：每条企业对的校验 + 评分完成

检查项：
  □ 分数区间：所有评分在 [0,100] 范围内？→ 否→❌阻断
  □ 红线触发：一票否决规则是否全部检查且匹配正确？→ 遗漏→❌阻断
  □ 红线→评级：红线触发后评级是否为 D/不通过？→ 否→❌阻断（最严重）
  □ 评级映射：综合分是否按规则映射到正确评级（A≥80/B 70-79/C 60-69/D<60）？→ 否→❌阻断
  □ 跨维度校验：四维权重加总 = 100%？→ 否→❌阻断

通过标准：全部检查项通过
拦截后果：该条不进入报告生成，输出错误详情
```

### 检查点③：复核反馈门

```
位置：用户提交复核意见后，写入记忆前
触发条件：每次人工复核提交

检查项：
  □ 完整性：复核意见是否包含 [修改了什么] + [为什么改]？→ 否→提示补充
  □ 差异记录：Agent初判 vs 人工修正的差异是否超过阈值(±10分)？→ 是→触发进化流程
  □ 写入校验：目标记忆文件是否可写？→ 否→❌阻断，提示磁盘/权限问题

通过标准：完整性检查通过
拦截后果：不完整的复核意见不写入记忆，提示用户补充
进化触发器：
  当 Agent初判与人工修正差异 > ±10分，自动记录为"待学习案例"
  累计 5 个待学习案例后，触发规则优化建议
```

---

## 验收标准（修订版）

### 功能验收

| 测试场景 | 输入 | 预期输出 | 测试方法 | 优先级 |
|---------|------|---------|---------|-------|
| TC-01 正常批次 | 5对正常企业的CSV | 输出判定结果+报告 | 使用demo中的5个案例数据 | P0 |
| TC-02 红线企业 | 1对红线触发企业 | 评级D/不通过，标注红线原因 | 使用远辰精密化工案例 | P0 |
| TC-03 空文件 | 空CSV | 拒绝并提示"无有效数据" | 上传空文件 | P0 |
| TC-04 格式错误 | 乱码/非标文件 | 拒绝并提示支持的格式 | 上传随机二进制 | P1 |
| TC-05 部分缺失 | 部分行缺少核心企业名 | 完整行正常判定，缺失行标记 | 手工构造 | P1 |
| TC-06 人工复核 | 对一条标记"待复核"的记录提交复核意见 | 记录保存，可查询，版本可追溯 | 命令行操作 | P0 |
| TC-07 记忆持久化 | 执行一批判定，关闭Claude Code，重新打开 | 历史记录可查询 | 启动前后对比 | P0 |
| TC-08 飞书推送 | 完成一批判定 | 飞书收到通知（或降级为本地归档） | 视权限可用性 | P2 |

### 性能验收

| 指标 | 标准 | 测量方式 |
|------|------|---------|
| 导入速度 | 100条 < 10秒（不含外部查询） | time 命令 |
| 单条企业判定 | < 30秒（含校验+评分+1次 WebSearch） | 日志时间戳差值 |
| 批量100条总耗时 | < 3分钟（含所有外部查询） | 整体计时 |
| 记忆查询 | 按企业名搜索 < 3秒 | 手动测试 |

### 绝对不能犯的错（0容忍）

| # | 错误 | 后果 | 对应检查点 |
|---|------|------|-----------|
| 🚫 E1 | 红线企业判定为通过 | 不合格企业进入银行授信流程 | 检查点② |
| 🚫 E2 | 评分数据/记忆张冠李戴（A企业的分给B） | 错误判定误导决策 | 每次写入校验企业ID |
| 🚫 E3 | 复核意见丢失/被覆盖 | 审计线索断裂，自进化失能 | Append-only 机制 |
| 🚫 E4 | 敏感信息被意外持久化 | 数据合规风险 | 检查点①过滤 |
| 🚫 E5 | LLM 自行"猜测"缺失数据生成评分 | 评分无据可查，违反监管要求 | T7规则引擎独立计算 |
| 🚫 E6 | 静默覆盖已有记忆/文件 | 数据不可逆丢失 | 版本号+写入前确认 |

### 测试数据集

```
demo 案例库（来自 核企智评_交互演示.html）：
  ① 华曜智能终端有限公司 × 北辰渠道科技有限公司  → 通过（d1:94, d3:90）
  ② 恒岳智慧家电有限公司   × 盛联商贸有限公司       → 通过（d1:80, d3:80）
  ③ 绿驰储能设备有限公司   × 东南能源渠道有限公司   → 待复核（d1:60, d3:60, 轻微处罚）
  ④ 新港工业材料有限公司   × 海岳分销有限公司       → 待复核（d1:53, d3:67, 资料不足）
  ⑤ 远辰精密化工有限公司   × 鑫达供应链有限公司     → 不通过（d1:87, d3:0, 税收违法红线）
  ⑥ 荣耀终端股份有限公司   × 北京松联科技有限公司   → 参考现有报告（79分, B级）

测试数据位置：tests/test_cases.json
```
